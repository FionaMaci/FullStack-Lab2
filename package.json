{
	"name": "Seminar2",
	"workspaces": [
		"server",
		"client"
	],
	"scripts": {
		"start": "npm run build && npm run server",
		"server": "cd server && npm start",
		"build": "cd client && npm run build",
		"add": "node ./server/test/test.js"
	},
	"dependencies": {
		"dotenv": "^16.5.0",
		"mongodb": "^6.16.0",
		"moongose": "^1.0.0",
		"node-fetch": "^3.3.2"
	}
}
